# 보관소 그리드 표시 개선 계획서 (액션 정렬/순서, 원본폴더, 크기, 소요시간)

## Goal Description
보관소 목록(`ArchiveListPage`)의 AG Grid 테이블 가독성과 조작성을 높이기 위해 다음 4가지 표시 요구사항을 반영합니다:
1. **액션 컬럼**: 왼쪽 정렬 및 아이콘 순서 재배치 (`미리보기` -> `다운로드` -> `삭제` -> `링크 복사` -> `링크 재발급`)
2. **원본폴더 컬럼**: `'폴더 #97'` 형태에서 `'폴더'` 단어를 제거하여 `'#97'` 형태로 간결화
3. **크기 컬럼**: 오른쪽 정렬 적용 (숫자/용량 데이터 표준 정렬)
4. **소요시간 컬럼**: `'소요'` 단어 제거 (`9.9초 소요` -> `9.9초`)

---

## User Review Required

> [!NOTE]
> **액션 아이콘 순서 및 정렬**
> * 셀 왼쪽부터 좌측 정렬(`justifyContent: 'flex-start'`)되며, 요청하신 순서대로 배치됩니다:
>   1. 👁️ **미리보기** (`Eye`)
>   2. ⬇️ **다운로드** (`Download`)
>   3. 🗑️ **삭제** (`Trash2`)
>   4. 🔗 **링크 복사** (`Link2`): 비공개 시 회색 비활성
>   5. 🔄 **링크 재발급** (`RotateCw`): 공개 상태일 때만 노출
> * 너비는 175px 유지되며 좌측 패딩(`px-2`)을 부여해 셀 경계선과 자연스럽게 떨어집니다.

> [!NOTE]
> **소요시간 포맷 (`formatElapsed`)**
> * [elapsedTime.ts](file:///home/kdy987/work/sofia2/frontend/src/shared/utils/elapsedTime.ts)의 `formatElapsed` 유틸 함수에서 `'소요'` 접미사를 제거하여 `${(ms / 1000).toFixed(1)}초`로 통일합니다.
> * 보관소 목록뿐 아니라 공유 페이지(`SharedArchivePage`), 작업 완료 패널(`OutputActionsPanel`)에서도 모두 불필요한 단어 없이 일관되게 `X.X초`로 표시됩니다.

---

## Proposed Changes

### 1. Utility Layer

#### [MODIFY] `frontend/src/shared/utils/elapsedTime.ts`
* `formatElapsed` 수정: `'소요'` 단어 제거
```typescript
// Before
export const formatElapsed = (ms: number) => `${(ms / 1000).toFixed(1)}초 소요`;

// After
export const formatElapsed = (ms: number) => `${(ms / 1000).toFixed(1)}초`;
```

---

### 2. Frontend Grid Layer

#### [MODIFY] `frontend/src/domain/archive/ArchiveListPage.tsx`

1. **원본 폴더 (`sourceFolderId`)**:
   * `'폴더 #' + params.value` -> `'#' + params.value`
```tsx
    {
      field: 'sourceFolderId',
      headerName: '원본 폴더',
      width: 100,
      filter: false,
      cellRenderer: (params: ICellRendererParams<ArchivedOutput>) =>
        params.value ? (
          <button
            className="text-blue-600 hover:underline text-sm font-medium"
            onClick={() => navigate(`/folder/${params.value}`)}
          >
            #{params.value}
          </button>
        ) : (
          <span className="text-gray-400 text-sm">-</span>
        ),
    },
```

2. **크기 (`fileSize`) 오른쪽 정렬**:
   * 헤더 및 셀 오른쪽 정렬 속성 부여
```tsx
    {
      field: 'fileSize',
      headerName: '크기',
      width: 110,
      filter: false,
      headerClass: 'ag-right-aligned-header',
      cellStyle: { textAlign: 'right' },
      valueFormatter: (p: ValueFormatterParams<ArchivedOutput>) => formatFileSize(p.value),
    },
```

3. **소요 시간 (`elapsedMs`)**:
   * 포맷터가 `formatElapsed`를 호출하므로 자동으로 `'9.9초'` 형태로 렌더링됨.

4. **액션 (`Action`) 컬럼 왼쪽 정렬 및 순서 변경**:
   * 정렬: `justifyContent: 'flex-start', paddingLeft: '8px'`
   * 순서: 미리보기 -> 다운로드 -> 삭제 -> 링크 복사 -> 링크 재발급
```tsx
    {
      headerName: '액션',
      width: 175,
      pinned: 'right',
      sortable: false,
      filter: false,
      resizable: false,
      cellStyle: { display: 'flex', alignItems: 'center', justifyContent: 'flex-start', paddingLeft: '8px' },
      cellRenderer: (params: ICellRendererParams<ArchivedOutput>) => {
        if (!params.data) return null;
        const item = params.data;
        const isPub = item.isPublic;

        return (
          <div className="flex items-center gap-1">
            {/* 1. 미리보기 */}
            <button
              title="미리보기"
              onClick={() => setPreviewItem(item)}
              className="w-7 h-7 rounded-md border border-emerald-300 text-emerald-600 hover:bg-emerald-50 hover:border-emerald-400 flex items-center justify-center cursor-pointer transition-colors"
            >
              <Eye size={14} strokeWidth={2} />
            </button>

            {/* 2. 다운로드 */}
            <button
              title="다운로드"
              onClick={() => handleDownload(item.id, item.displayFilename)}
              className="w-7 h-7 rounded-md border border-blue-300 text-blue-600 hover:bg-blue-50 hover:border-blue-400 flex items-center justify-center cursor-pointer transition-colors"
            >
              <Download size={14} strokeWidth={2} />
            </button>

            {/* 3. 삭제 */}
            <button
              title="보관소에서 삭제"
              onClick={() => handleDelete(item.id, item.displayFilename)}
              className="w-7 h-7 rounded-md border border-red-300 text-red-600 hover:bg-red-50 hover:border-red-400 flex items-center justify-center cursor-pointer transition-colors"
            >
              <Trash2 size={14} strokeWidth={2} />
            </button>

            {/* 4. 공유 링크 복사 */}
            {isPub ? (
              <button
                title="공유 링크 복사"
                onClick={() => handleCopyShareLink(item.shareKey)}
                className="w-7 h-7 rounded-md border border-indigo-300 text-indigo-600 hover:bg-indigo-50 hover:border-indigo-400 flex items-center justify-center cursor-pointer transition-colors"
              >
                <Link2 size={14} strokeWidth={2} />
              </button>
            ) : (
              <button
                title="비공개 상태입니다 (먼저 공개로 설정해주세요)"
                disabled
                className="w-7 h-7 rounded-md border border-gray-200 text-gray-300 flex items-center justify-center cursor-not-allowed bg-gray-50/50"
              >
                <Link2 size={14} strokeWidth={2} />
              </button>
            )}

            {/* 5. 새 링크 재발급 (공개 시에만 활성화) */}
            {isPub && (
              <button
                title="새 링크로 재발급 (이전 링크 무효화)"
                onClick={() => handleReissueShareKey(item)}
                className="w-7 h-7 rounded-md border border-amber-300 text-amber-600 hover:bg-amber-50 hover:border-amber-400 flex items-center justify-center cursor-pointer transition-colors"
              >
                <RotateCw size={13} strokeWidth={2} />
              </button>
            )}
          </div>
        );
      },
    },
```

---

## Verification Plan

### Automated Tests
```bash
./fm.sh lint
./fm.sh build
./bm.sh build
```

### Manual Verification
1. **액션 컬럼**:
   * 셀 내 버튼들이 왼쪽 정렬되어 있는지 확인.
   * 버튼 순서가 `미리보기` -> `다운로드` -> `삭제` -> `링크 복사` -> `링크 재발급` 순서인지 확인.
2. **원본폴더**:
   * '폴더' 단어가 빠지고 `#97` 형태로 간결하게 노출되는지 확인. 클릭 시 해당 폴더로 정상 이동 확인.
3. **크기 컬럼**:
   * 헤더 및 셀 텍스트가 모두 오른쪽 정렬되어 숫자가 깔끔하게 정렬되는지 확인.
4. **소요시간 컬럼**:
   * `'소요'` 단어가 제거되어 `9.9초`, `2.3초` 형태로 표시되는지 확인.
